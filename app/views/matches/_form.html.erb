<%= form_with(model: match) do |form| %>
  <% if match.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
        <% match.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :first_user_id, style: "display: block" %>
    <%= form.text_field :first_user_id %>
  </div>

  <div>
    <%= form.label :second_user_id, style: "display: block" %>
    <%= form.text_field :second_user_id %>
  </div>

  <div>
    <%= form.label :target_id, style: "display: block" %>
    <%= form.text_field :target_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
